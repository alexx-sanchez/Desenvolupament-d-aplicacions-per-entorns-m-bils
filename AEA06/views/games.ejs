<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Benvingut</title>
    <link rel="stylesheet" type="text/css" href="/styles.css">
</head>

<body>
    <header>
        <h1>Gestió de Jocs per <%= user.name %>!</h1>
        <h2><%- htmlMessage %></h2>
    </header>
    <main>
        <section>
            <h2>Llista de Jocs</h2>
            <ul class="product-list">
                <% data.games.forEach(game=> { %>
                    <li class="product-item">
                        <strong>
                            <%= game.name %>
                        </strong>
                        <span>Categoria: <%= game.category %></span>
                        <span>Preu: <%= game.price %> €</span>
                        <span><a href="/games/<%=game.id %>">Detall Joc</a></span>
                        <span><a href="/games/editGame/<%=game.id %>">Editar joc</a></span>
                        <span>
                            <form action="/games/<%= game.id %>?_method=DELETE" method="POST" style="display:inline;">
                                <button type="submit" class="link-button">Eliminar Joc</button>
                            </form>
                        </span>

                    </li>
                    <% }) %>
            </ul>
        </section>
    </main>
</body>

</html>