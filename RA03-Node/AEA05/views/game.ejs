<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detall del Joc</title>
    <link rel="stylesheet" type="text/css" href="/css/detail.css">
</head>

<body>
    <header>
        <h1>Gestió de Jocs per <%= user.name %>!</h1>
        <a href="/games" class="back-button">Tornar al llistat</a>
    </header>

    <main>
        <section class="detail-section">
            <h2>Detall del Joc</h2>
            <div class="detail-card">
                <% if (game.photo) { %>
                    <img src="<%= game.photo %>" alt="Foto de <%= game.name %>" class="item-photo">
                <% } else { %>
                    <img src="/default-game.png" alt="Foto por defecto" class="item-photo">
                <% } %>

                <h3 class="item-name"><%= game.name %></h3>
                <p><strong>Categoria:</strong> <%= game.category %></p>
                <p><strong>Preu:</strong> €<%= game.price %></p>

                <div class="action-buttons">
                    <form action="/games/editGame/<%= game.id %>" method="GET">
                        <button type="submit" class="action-button edit-button">Editar Joc</button>
                    </form>

                    <form action="/games/<%= game.id %>?_method=DELETE" method="POST">
                        <button type="submit" class="action-button delete-button">Eliminar Joc</button>
                    </form>
                </div>
            </div>
        </section>
    </main>
</body>

</html>
