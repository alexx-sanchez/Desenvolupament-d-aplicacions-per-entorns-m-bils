<!DOCTYPE html>
<html lang="ca">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edita Joc</title>
    <link rel="stylesheet" type="text/css" href="/css/styles.css">
</head>

<body>
    <header>
        <h1>Editar Joc: <%= game.name %></h1>
    </header>

    <main class="container">
        <div class="card-item">
            <!-- Preview de la imagen -->
            <% if (game.photo) { %>
                <img src="<%= game.photo %>" alt="<%= game.name %>" class="item-photo" style="width:150px; height:150px; object-fit:cover; border-radius:50%; margin:0 auto 1rem;">
            <% } %>

            <form action="/games/<%= game.id %>?_method=PUT" method="POST" class="form-card">
                <label for="name">Nom:</label>
                <input type="text" id="name" name="name" value="<%= game.name %>" required>

                <label for="category">Categoria:</label>
                <input type="text" id="category" name="category" value="<%= game.category %>" required>

                <label for="price">Preu (€):</label>
                <input type="number" id="price" name="price" value="<%= game.price %>" step="0.01" required>

                <label for="photo">URL Foto:</label>
                <input type="url" id="photo" name="photo" value="<%= game.photo %>" placeholder="https://example.com/foto.jpg">

                <button type="submit" class="btn-primary">Desar Canvis</button>
            </form>

            <a href="/games" class="nav-link">Tornar al llistat</a>
        </div>
    </main>
</body>

</html>
